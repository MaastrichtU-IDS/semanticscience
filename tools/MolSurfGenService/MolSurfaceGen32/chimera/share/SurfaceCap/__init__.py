# -----------------------------------------------------------------------------
# Extension to cap surfaces (_surface.SurfaceModel) at per-model and near
# clip planes.
#
from surfcaps import enable_capping, is_surface_cap, cap_model
